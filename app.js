//// Code to parse the output from terminal after comand:  git ls-remote  // should be run from git-initialized directory
//// run with node

// var fs = require('fs')
// var output = fs.readFileSync('file.txt', 'UTF8')
// 	.trim()
// 	.split('\n')
// 	.map(function(line){ return line.split('/')})
// 	.reduce(function(users, line) {
// 		users[line[2]] = users[line[2]] || []
// 		users[line[2]].push({
// 			hash: line[0]
// 		})
// 		return users }, {})
//  console.log('output', JSON.stringify(output, null, 2));
